{
  "name": "ember-ts",
  "version": "0.1.0",
  "description": "TypeScript client for the ember cache server over gRPC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"no tests yet\"",
    "proto-gen": "npx --package=@grpc/proto-loader proto-loader-gen-types --proto_path=../../proto --outDir=src/generated ../../proto/ember/v1/ember.proto"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.12.0",
    "@grpc/proto-loader": "^0.7.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
